import { CustomMinDirective } from './custom-min-directive';

describe('CustomMinDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomMinDirective();
    expect(directive).toBeTruthy();
  });
});
