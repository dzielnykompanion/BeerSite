import { CustomMaxDirective } from './custom-max-directive';

describe('CustomMaxDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomMaxDirective();
    expect(directive).toBeTruthy();
  });
});
